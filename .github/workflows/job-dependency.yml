name: job-dependency.yml
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compilation"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Testing"

  deploy:
    runs-on: ubuntu-latest
    needs: [build, test]
    steps:
      - run: echo "Deploying"
